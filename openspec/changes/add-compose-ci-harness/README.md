# add-compose-ci-harness

Containerized CI harness: docker-compose runs pytest in a python container and Playwright browser tests in a node container, driven by the Makefile that GitHub Actions also runs (runnable locally)
